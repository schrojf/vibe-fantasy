{
  "name": "vibe-fantasy",
  "version": "1.0.0",
  "description": "HTML5 Canvas engine pre VF-like hru s podporou regiónov a editorom",
  "scripts": {
    "predeploy-gh-pages": "rm -rf dist && mkdir dist && cp *.html *.js *.json *.png *.ico *.webmanifest dist/ 2>/dev/null || true",
    "deploy-gh-pages": "gh-pages -d dist -t --nojekyll",
    "start": "live-server --port=8080 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "gh-pages": "^6.1.1"
  }
}
