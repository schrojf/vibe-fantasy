{
  "name": "ff-canvas-hra",
  "version": "1.0.0",
  "description": "HTML5 Canvas engine pre FF-like hru s podporou regiónov a editorom",
  "scripts": {
    "start": "live-server --port=8080 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
